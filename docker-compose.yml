services:
  migrate:
    build: .
    container_name: icu.xiaosong.blog.service.migrate
    env_file: ./compose/.local.env
    environment:
      - PYTHONPATH=/app
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - .:/app
    command: alembic upgrade head

  api:
    build: .
    container_name: icu.xiaosong.blog.service.api
    env_file: ./compose/.local.env
    environment:
      - PYTHONPATH=/app
    depends_on:
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_started
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - ./app/storage:/app/storage
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  db:
    image: zhparser/zhparser:bullseye-16
    container_name: icu.xiaosong.blog.service.db
    env_file: ./compose/.local.env
    ports:
      - "5432:5432"
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB" ]
      interval: 5s
      timeout: 5s
      retries: 20
    volumes:
      - pg_data:/var/lib/postgresql/data
      # 如果你要自动建表，把 init.sql 放到 ./db/init.sql，然后打开下面这行
      # - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql

  redis:
    image: redis:8-alpine
    ports:
      - "6379:6379"
    command: [ "redis-server", "/usr/local/etc/redis/redis.conf" ]
    volumes:
      - ./compose/store/local.redis.conf:/usr/local/etc/redis/redis.conf:ro
      - redis_data:/data

volumes:
  pg_data:
  redis_data:
